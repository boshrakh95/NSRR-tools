
1)  Group figures together as subplots so avoid many single figures. Do it for figures that make sense to come together and their interpretations are better merged. The subplots can come either from one directory and task or from multiple directories. I want an efficient presentation of the results to facilitate interpretation across tasks, heads, contexts, modes (minimal channel, full channel, modality ablation). 
2)  For figures that should be merge into one group, write one interpretation explaining everything and whenever needed, across-subplot comparisons/justifications.
3)  I want to improve axes text and legends, remove figure titles and add the needed details to figure captions. Also review font, size, colors, styles and make sure they are TBME recommended styles (check md file). Add every necessary note to the additional comments column so that I can use later to change the figure generation code. These notes should also contain any necessary instructions for merging plots into one that you decided.
4)  Look at the generation codes to understand in details how each figure is generated and what each thing represents. If needed, review methods section to remember all definitions because some Ks and other parameters should be interpreted with care. So the figures should be interpreted based on all hypotheses we made, and all methods details, not just looking at that figure alone.
5)  When each figure’s results don’t make sense (unjustifiable) or are very bad, mark them in the table so that we possibly don’t include them in paper (do write their interpretation but bold out the interpretation of that plot or subplot so that I remove if I want)
6)  These are the figures I want to exclude completely from the paper (for all tasks): *_calibration_2C_ece_vs_k.png, sex_binary_lstm_kstar_9B_coverage.png, *_subject_consistency_5B_variance_vs_k.png (write interpretation if their results make sense but mark them as black list)
-       Some notes for each figure:
7)  *_subject_consistency_5C_hard_subjects.png does not look easy to understand for the user. I want either change of axes text or of the way we presenting the results 
8)  Legend of sex_binary_lstm_cohort_saturation_7B_n.png should be modified to not include mros. Legends of all plots should be checked and written with care.
9)  For *_test_window_sweep_auroc.png let’s keep mv or mp, one that makes more sense or has better results.
10)   We should keep either *_test_window_sweep_auroc.png or *_test_window_sweep_balanced_accuracy.png decide based on the results. Let’s keep the one that has best results over all tasks.
11)   Do write the interpretations for figures inside both [task_head]/auroc_test/*.png and [task_head]/balanced_accuracy_test/*.png but at the end we must keep one group. Mark interpretation of figures of the group you chose to exclude in 10 so that I remember that I’ll probably put them in supplementary if the results do not contradict with the group that we include, otherwise I’ll exclude them.
12)   We should do the same for figures in saturation folder. Either BA or AUROC based on the results (one decision for all the figures). Again do the interpretation for both metrics but mark one for possible exclusion.
13)   If the age_class_1C_optimal_epoch.png results of most tasks seem random and not easily justifiable, recommend to exclude this plot.
14)   Do interpret current *_1A_uShape.png figures, but flag that I need to change code to run it based on BA and not loss because our training early stopping was based on that metric. After I generate those plots, I will ask you again to interpret.